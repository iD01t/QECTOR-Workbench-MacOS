[pytest]
# Collect only the real test package.  test_mcp_all.py at the repo root is a
# standalone MCP verification *script* (run directly with `python test_mcp_all.py`),
# not a pytest module, so it must be excluded from collection.
testpaths = tests
asyncio_mode = auto
addopts = -q --tb=short
