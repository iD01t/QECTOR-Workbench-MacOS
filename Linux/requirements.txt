customtkinter>=5.2.0
# numpy must stay <2.3: the qector-decoder-v3 0.6.9 wheel declares
# numpy>=1.24.0,<2.3, and its compiled Rust bindings reject arrays from newer
# numpy builds (NeuralPredecoder.train raised TypeError under numpy 2.5.1).
numpy>=1.24,<2.3
qector-decoder-v3==1.0.0
scipy>=1.9
matplotlib>=3.8
Pillow>=9.0
psutil>=5.9
# Runtime dependency of the externally provisioned decoder (qector-decoder-v3
# >= 0.6.8 imports `cryptography` at package import time for license checks).
# The decoder is installed with --no-deps, and the frozen app does not otherwise
# import cryptography, so it MUST be bundled here or `import qector_decoder_v3`
# fails at launch inside the frozen app.
cryptography>=41.0
# In-app "Export Official Docs" generates the full public documentation set
# (user manuals, MCP guide, quick start, API reference) as PDFs via reportlab.
reportlab>=4.0
# DOCX companions for the Markdown manuals. Optional: when it is missing the
# exporter reports the skip and still writes every other format. It must never
# be worked around by renaming a text file to .docx.
python-docx>=1.1.0
